providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python -m venv /opt/venv", ". /opt/venv/bin/activate", "pip install -r ml/requirements.txt"]

[start]
cmd = ". /opt/venv/bin/activate && uvicorn ml.api.main:app --host 0.0.0.0 --port $PORT"
